<?xml-model href="http://docbook.org/xml/5.0/rng/docbook.rng"
        schematypens="http://relaxng.org/ns/structure/1.0"?><?xml-model href="http://docbook.org/xml/5.0/rng/docbook.rng" type="application/xml"
        schematypens="http://purl.oclc.org/dsdl/schematron"?>
<article xmlns="http://docbook.org/ns/docbook" version="5.0">
   <info>
      <title>Database Deployment</title>
      <date>February 2010</date>
      <keywordset>
         <keyword>TBD</keyword>
      </keywordset>
   </info>

   <!-- ================================================================== -->

   <sect1>
      <title>Overview</title>

      <para>There are <emphasis>three</emphasis> ways to deploy the eXist-db database:</para>
      <orderedlist>
         <listitem>
            <para>
                        <emphasis>In a Servlet Context</emphasis>
                    </para>
            <para>In this case, the database is deployed as part of a web application. eXist
                        will happily live together with other servlets. This is
                        the default mode established when the database has been deployed according
                        to instructions provided in the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="quickstart.xml">Quick Start
                            Guide</link>.</para>
            <para>All resources used by eXist-db in this mode will have paths relative to the
                        web application's current context. For example, eXist will store all its
                        database files in the <literal>WEB-INF/data</literal> directory of the web
                        application.</para>
         </listitem>
         <listitem>
            <para>
                        <emphasis>Stand-alone Server Process</emphasis>
                    </para>
            <para>In stand-alone mode, eXist-db runs in its own Java virtual machine (JVM).
                        Clients have to access the database through the network, either using the
                        XML-RPC or WebDAV protocol or the REST-style HTTP API (see the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="devguide.xml">Developer's Guide</link>).</para>
         </listitem>
         <listitem>
            <para>
                        <emphasis>Embedded in an Application</emphasis>
                    </para>
            <para>In embedded mode, the database is basically used as a Java library,
                        controlled by the client application. It runs in the same Java virtual
                        machine as the client, thus no network connection is needed and the client
                        has full access to the database.</para>
         </listitem>
      </orderedlist>
      <para>Note that all three deployments are thread-safe and allow concurrent operations by
                multiple users. Also note that servlets running in the same web application context
                will have direct access to the database. External client applications may still 
                use the supplied network interfaces.</para>
      <para>As detailed instructions on how to set up eXist-db for use with a servlet-engine are
                provided in the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="quickstart.xml">Quick Start Guide</link>, the sections
                in this document concern the other two deployment options. In section 2, we
                introduce the XML:DB URI and explain how different servers are addressed by Java
                clients. Section 3 deals with running eXist as a stand-alone server, and in Section
                4 we discuss the required steps to directly embed eXist into an application,
                including how to embed in the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="http://titanium.dstc.edu.au">XMLdbGUI</link>.</para>
   </sect1>

   <!-- ================================================================== -->

   <sect1>
      <title>Addressing Different Servers using the XML:DB URI</title>

      <para>One way to access eXist-db from Java applications is to use the XML:DB API. The
                XML:DB API uses a specific URI scheme to locate a collection of XML resources on the
                server. You will encounter XML:DB URIs when working with the Java client, the backup
                tools and sometimes in XQuery functions. It is thus important to understand how the
                URI scheme addresses servers and resources.</para>
      <para>eXist's XML:DB API implementation supports transparent access to remote as well as
                embedded database servers. This means the database on the server is available on the
                client as though it were locally connected to the client - i.e. the user should not
                have to be aware of where a resource is physically located. Given this transparency,
                applications need not be affected by how the database has been deployed.</para>
      <para>The XML:DB URI identifies the database implementation, the name of the collection,
                and optionally the location of the database server on the network. For example, the
                URI:</para>
      <programlisting>xmldb:exist://localhost:8080/exist/xmlrpc/db/shakespeare</programlisting>
      <para>points to the shakespeare-collection on a remote server which is running in a
                servlet-engine. The host part: <literal>localhost:8080/exist/xmlrpc</literal>
                describes the path to the XML-RPC listener, which is running as a servlet. The
                collection part (<literal>/db/shakespeare</literal>) defines the collection to
                retrieve. If eXist had started as a stand-alone server, the URI would change its
                port to <literal>8088</literal> to become:</para>
      <programlisting>xmldb:exist://localhost:8088/xmlrpc/db/shakespeare</programlisting>
      <para>Finally, to access an embedded instance of the database, we simply drop the host
                part altogether, and use three forward slashes (<literal>///</literal>) in front of
                the collection path:</para>
      <programlisting>xmldb:exist:///db/shakespeare</programlisting>
      <para>You can use the command-line client to experiment with these settings. The client
                utilizes the XML:DB API to access the database. The <literal>Login</literal> dialog
                offers a text field where you can specify an XML:DB URI to connect to. The default
                URI shown here is configured through a properties-file called <literal>client.properties</literal>. By default, the client tries to
                access the database engine located at the base URI:</para>
      <programlisting>xmldb:exist://localhost:8080/exist/xmlrpc/</programlisting>
      <para>To use the client with another server, you can simply change the
                    <literal>uri</literal> property for the server location. For permanent changes,
                edit the properties-file <literal>client.properties</literal>. To
                make temporary changes, pass the <literal>-ouri</literal> option on the command line.
                For example, if you start the Admin client using the command:</para>
      <programlisting>bin/client.sh -ouri=xmldb:exist://</programlisting>
      <para>this will cause a local database instance to run in the same Java virtual machine
                as the client. Note that the short form for this particular command uses the
                    <literal>-l</literal> option that causes the client to launch a local database
                instance - i.e.:</para>
      <programlisting>bin/client.sh -l</programlisting>
   </sect1>

   <!-- ================================================================== -->

   <sect1>
      <title>Deploying eXist-db in a Servlet Container</title>

      <para>The standard distribution does by default launch eXist-db as a web application
                deployed in a servlet container (if you start eXist via <literal>bin/startup.sh</literal> or <literal>bin\startup.bat</literal>). The
                container used is <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="http://jetty.mortbay.org/">Jetty</link>.</para>
      <para>The Jetty configuration can be found in <literal>tools/jetty/webapps/exist-webapp-context.xml</literal>.
                It defines a single web application context value <literal>&lt;Set name="contextPath"&gt;/exist&lt;/Set&gt;</literal>, mapped to the
                    <literal>webapp</literal> directory. This value can be set to either <literal>/exist</literal> or 
		    <literal>/</literal>.</para>
      <para>We think that Jetty is small, efficient and stable, so there's no need to switch
                to a different servlet engine unless your server setup requires this. However, it is
                not difficult to deploy eXist in a different servlet container, e.g. Apache's
                Tomcat.</para>
      <para>We usually provide a <literal>.war</literal> web archive for download for all
                major releases. You may either download that or - if you already installed the
                standard distribution - build a .war archive yourself. For building the .war, you
                need the eXist sources. In the installer GUI, include the "source" module when
                selecting installable packages. To build the .war file, just call the main build
                script (<literal>build.sh</literal> or <literal>build.bat</literal>) with the
                target <literal>dist-war</literal>:</para>
      <programlisting>build.sh dist-war</programlisting>
      <para>To install the .war, just copy it into the corresponding folder below your servlet
                engine installation (usually <literal>webapps</literal>) and rename it to
                    <literal>exist.war</literal>. The servlet engine will normally unpack the file
                when you restart the server. To have better control of where the file is unpacked,
                you can also create a directory <literal>exist</literal> below the <literal>webapps</literal> folder and manually extract the .war there, e.g.
                with:</para>
      <programlisting>jar xfv exist.war</programlisting>
      <important>
         <para>Please remember that the <literal>data</literal> and <literal>log</literal> directories need to be writable!</para>
      </important>
      <para>There are a few differences between the standard distribution and the .war
                install: in particular, the configuration files are found in the <literal>WEB-INF</literal> directory, including the important files <literal>conf.xml</literal> (eXist configuration) and <literal>log4j.xml</literal>
                (logging). The example data is in <literal>samples</literal>.</para>
      <para>To start the Java admin client (described in the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="quickstart.xml">QuickStart</link>), use the following command line (from within the <literal>exist</literal> web application root directory:</para>
      <programlisting>java -jar WEB-INF/lib/start.jar client -ls</programlisting>
   </sect1>

   <!-- ================================================================== -->

   <sect1>
      <title>Running eXist-db as a Stand-alone Server</title>

      <para>There are many cases in which it is preferable to have the database engine running
                in its own Java virtual machine. In stand-alone mode, eXist will launch its own,
                embedded web server to provide XML-RPC, WebDAV and REST services to the outside
                world. The embedded server is based on a stripped-down Jetty. It uses a limited
                configuration, excluding all the additional services available in a full-blown
                servlet environment.</para>
      <para>In general, the stand-alone deployment is more reliable and performant than the
                web application setup, since no other threads (simultaneous tasks) are running. If
                your application does not need WebDAV or SOAP, you should use the stand-alone
                server.</para>
      <para>The stand-alone database server offers XML-RPC, WebDAV and REST-style HTTP
                interfaces for external client access. Please note that it does not support SOAP.
                The XML-RPC and REST interfaces are explained in-depth in the <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="devguide.xml">Developer's Guide</link>.</para>
      <para>By default, the stand-alone server listens on port <literal>8088</literal>,
                though this can be changed (see configuration below). The Java class for launching
                the server is <literal>org.exist.jetty.StandaloneServer</literal>.</para>

      <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->

      <sect2>
         <title>Starting the Stand-alone Server</title>

         <para>To start the server, launch either the Unix shell script:</para>
         <programlisting>bin/server.sh</programlisting>
         <para>OR the Windows/DOS batch file:</para>
         <programlisting>bin\server.bat</programlisting>
         <para>The server is multi-threaded - a server thread is assigned to each client
                    request. If the specified maximum number of threads is reached, the server will
                    block the client until one of the server-threads is available to respond. By
                    default, the maximum number of threads is 5. To change this, use the
                        <literal>-t</literal> option, e.g.:</para>
         <programlisting>bin\server.bat -t 20</programlisting>
         <para>To access the stand-alone server using the interactive command line client (or
                    by your own Java classes), you must change the XML:DB server URI set by the
                        <literal>uri</literal> property, as described above, to the following:</para>
         <programlisting>xmldb:exist://localhost:8088/xmlrpc</programlisting>
      </sect2>

      <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->

      <sect2>
         <title>StandaloneServer Configuration with tools/jetty/etc/standalone/WEB-INF/web.xml</title>

         <para>The <literal>tools/jetty/etc/standalone/WEB-INF/web.xml</literal> configuration file is used when the
                    server operates in standalone mode, in which case, an instance of the Jetty web
                    server is configured using its settings. As for the services offered with eXist,
                    each has its own configuration servlet element in this file. Currently, the servlet API
                    alternatives are "<literal>webdav</literal>, "<literal>xmlrpc</literal>", and
                        "<literal>rest</literal>". Any of these servlets can be disabled by setting
                    a init-param <literal>enabled</literal> attribute to "<literal>no</literal>".</para>
         <para>The standalone.xml/web.xml document has the following basic structure:</para>
         <example>
            <title>Sample Configuration File: web.xml</title>
            <programlisting language="xml">fixme!/ljo</programlisting>
         </example>
         <sect3>
            <title>Controlling the Binding Address (&lt;indexer&gt;)</title>

            <para>You can control the binding address by changing the addConnector <tag>Call</tag> element in tools/jetty/etc/standalone.xml:</para>
            <variablelist spacing="compact">
               <varlistentry>
                  <term>port</term>
                  <listitem>
                     <para>The port on which the server will listen. The Jetty web server
                                    will bind to the port <literal>8088</literal>
                                    by default.</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>host</term>
                  <listitem>
                     <para>The hostname on which the server will respond.</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>address</term>
                  <listitem>
                     <para>The IP address on which the server should bind. This may be
                                    useful when the server has multiple addresses that serve the
                                    same hostname.</para>
                  </listitem>
               </varlistentry>
            </variablelist>
         </sect3>
         <sect3>
            <title>WebDAV Servlet</title>

            <para>The WebDAV servlet provides services at the context address specified.
                        This servlet is represented by the <tag>webdav</tag> element in the
                            <literal>server.xml</literal> configuration file. For this element,
                        the "<literal>context</literal>" attribute controls the web server context at
                        which the WebDAV services are provided. If the "<literal>context</literal>"
                        attribute is not specified, it defaults to <literal>/webdav/</literal>.</para>
         </sect3>
         <sect3>
            <title>XML-RPC Servlet</title>

            <para>The XML-RPC servlet provides database API services to clients like the
                        Admin GUI client. This servlet is represented by the
                            <tag>xmlrpc</tag> element in the <literal>server.xml</literal>
                        configuration file. For this element, the "<literal>context</literal>"
                        attribute controls the web server context at which the XMLRPC services are
                        provided. If the "<literal>context</literal>" attribute is not specified, it
                        defaults to <literal>/xmlrpc/</literal>. Note that if you disable this
                        servlet, you effectively disable the use of the admin client.</para>
         </sect3>
         <sect3>
            <title>REST Servlet</title>

            <para>NB! Updated for Jetty 7 in trunk from December 2009</para>
            <para>The REST servlet provides HTTP/REST-style interactions with the database.
                        It is configured by a <tag>servlet</tag> element with  <tag>servlet-class</tag>org.exist.http.servlets.EXistServlet in the <literal>tools/jetty/etc/standalone/WEB-INF/web.xml</literal> configuration file. It depends on the XQueryURLRewrite filter for the context, defaults is <literal>/</literal>.</para>
            <para>The RestServlet has a number of other parameters that can be
                        set by child elements:</para>
            <example>
               <title>RestServlet Structure</title>
               <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="xml" xlink:href="listings/listing-15.xml"/>
            </example>
            <variablelist spacing="compact">
               <varlistentry>
                  <term>form-encoding</term>
                  <listitem>
                     <para>The default encoding of form POSTs.</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>container-encoding</term>
                  <listitem>
                     <para>The default encoding of the servlet container for all HTTP
                                    interactions except POSTs.</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>use-default-user</term>
                  <listitem>
                     <para>A boolean value (<literal>true</literal>/<literal>false</literal>)
                                    that indicates whether a user you supply the credentials 
				    <literal>user</literal> and <literal>password</literal> for 
				    or if the <literal>guest</literal> user with default password should be
                                    used for non-authenticated interactions. 
				    If any of these fails a simple AUTH interaction is undertaken.
				</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>user</term>
                  <listitem>
                     <para>The username of the default user.</para>
                  </listitem>
               </varlistentry>
               <varlistentry>
                  <term>user</term>
                  <listitem>
                     <para>The password of the default user (required when every users
                                    has a password).</para>
                  </listitem>
               </varlistentry>
            </variablelist>
         </sect3>
         <sect3>
            <title>Custom Servlets</title>

            <para>Any servlet can be configured to run in the Jetty server by adding a
                        'servlet' element to the server.xml configuration file. The 'servlet'
                        element has the standard attributes of 'enabled' and 'context' as well as
                        the 'class' attribute to specify the servlet implementation class.</para>
            <para>This element can have any number of <literal>param</literal> element
                        children to set parameters on the servlet. The structure looks like:</para>
            <example>
               <title>&lt;rest&gt;-&lt;param&gt; Structure</title>
               <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="xml" xlink:href="listings/listing-16.xml"/>
            </example>
         </sect3>
         <sect3>
            <title>Forwarding Requests</title>

            <para>The forwarding-request settings allow you to map incoming URL requests to
                        specific resources on the server (e.g. queries). All of which are
                        encapsulated in the <tag>forwarding</tag> element. Inside this
                        element is a single <tag>root</tag> element and any number of
                            <tag>forward</tag> elements. Each of these latter elements
                        specifies a specific URL path that is forwarded to secondary URL path. This
                        allows you to map a "clean" URL to an XQuery or some other resource within
                        the server. The target is always specified by a <literal>destination</literal>
                        attribute.</para>
            <para>The <tag>root</tag> element maps requests to the root directory of
                        the server (i.e. the "<literal>/</literal>" path) to a specified resource.
                        For example:</para>
            <example>
               <title>Root Map (&lt;root&gt;)</title>
               <programlisting language="xml">&lt;root destination="/db/admin/admin.xql" /&gt;</programlisting>
            </example>
            <para>The above element maps requests for the (default) server root to the
                        XQuery resource <literal>/db/admin/admin.xql</literal>.</para>
            <para>The <tag>forward</tag>element, on the other hand, maps the request
                        specified by the <literal>path</literal> attribute to a resource. For
                        example:</para>
            <example>
               <title>Forward Maps (&lt;forward&gt;)</title>
               <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="xml" xlink:href="listings/listing-18.txt"/>
            </example>
            <para>In this example, the first element (&lt;forward&gt;) maps the URI path
                            <literal>/admin</literal> to the XQuery resource <literal>/db/admin/admin.xql</literal>, while the second element maps the URI
                        path <literal>/docs</literal> to the document <literal>/db/products/docs.xml</literal>.</para>
         </sect3>
      </sect2>

      <!-- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->

      <sect2>
         <title>Shutting Down the Database</title>

         <para>By default, the <literal>shutdown.bat</literal> (Windows/DOS) and <literal>shutdown.sh</literal> (Unix) scripts try to connect to the default server
                    URI - i.e.:</para>
         <programlisting>xmldb:exist://localhost:8080/exist/xmlrpc</programlisting>
         <para>If your database is running in stand-alone mode, you must specify a different
                    server URI. Specifically, to stop eXist when running in stand-alone mode
                    listening on port <literal>8088</literal>, use the following:</para>
         <programlisting>java -jar start.jar shutdown --uri=xmldb:exist://localhost:8088/xmlrpc</programlisting>
      </sect2>
   </sect1>

   <!-- ================================================================== -->

   <sect1>
      <title>Embedding eXist in an Application</title>

      <para>In the embedded mode, the database runs in the same Java virtual machine as the
                client application. The database will not be accessible by any outside application,
                and no network listeners are started.</para>
      <para>You can embed eXist into any Java application using the XML:DB API. Other APIs
                might be added in the future. In particular, we are currently working to implement
                JSR 225: the "XQuery API for Java" (XQJ). However, until alternatives are available,
                we recommend using the XML:DB.</para>
      <para>To prepare the environment for using an embedded eXist, follow the steps
                below:</para>
      <orderedlist>
         <listitem>
            <para>Copy <literal>conf.xml</literal> and <literal>log4j.xml</literal> to
                        the target directory.</para>
         </listitem>
         <listitem>
            <para>Create a subdirectory <literal>data</literal> in the target directory.
                        Edit the <literal>files</literal> attribute in the
                            <literal>db-connection</literal> section of <literal>conf.xml</literal>
                        to point to this data directory. Do the same for the
                            <literal>journal-dir</literal> in the <literal>recovery</literal>
                        element.</para>
            <para>To see logging output, edit <literal>log4j.xml</literal>. The simplest
                        way is to change the <literal>appender-ref</literal> in the
                            <tag>root</tag> category to <literal>"console"</literal>, which
                        will result in most log messages being written to the console.</para>
         </listitem>
         <listitem>
            <para>Create a <literal>lib</literal> directory below the target directory and
                        copy the following <literal>.jar</literal> files from eXist-db into it:</para>
            <itemizedlist>
               <listitem>
                  <para>
                                <literal>exist.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/extensions/exist-modules.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/antlr-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/commons-pool-X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/commons-collections-X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/commons-logging-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/log4j-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/optional/slf4j-api-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/optional/slf4j-log4j12-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/quartz-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/sunxacml.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/xmldb.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/xmlrpc-client-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/xmlrpc-common-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/xmlrpc-server-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/jta.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lib/core/pkg-repo.jar</literal>
                            </para>
               </listitem>
            </itemizedlist>
            <para>If you plan to use extension modules (like the n-gram index), you also
                        need to copy the corresponding jars from <literal>lib/extensions</literal>. The default eXist configuration needs:</para>
            <itemizedlist>
               <listitem>
                  <para>
                                <literal>exist-ngram-module.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>exist-lucene-module.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>exist-versioning.jar</literal>
                            </para>
               </listitem>
            </itemizedlist>
            <para>For the Lucene module you also need to copy the jars from <literal>extensions/indexes/lib</literal>:</para>
            <itemizedlist>
               <listitem>
                  <para>
                                <literal>lucene-core-X.X.X.jar</literal>
                            </para>
               </listitem>
               <listitem>
                  <para>
                                <literal>lucene-regex-X.X.X.jar</literal>
                            </para>
               </listitem>
            </itemizedlist>
            <para>The <literal>lib/endorsed</literal> directory furthermore plays a
                        special role: the Java releases come with their own XML support libraries,
                        including Xalan for XSLT processing, an XML parser, and the standard Java
                        interfaces for SAX and DOM. Some features of eXist will not work properly
                        with a wrong version of Xerces or the resolver jar (schema validation,
                        catalog loading, ...). To ensure that the correct versions are available, we
                        have included these versions of Xerces, Xalan and Saxon, plus the standard
                        interfaces used by both of them. You can use Java's endorsed library loading
                        mechanism to ensure that the correct XML support libraries are loaded. </para>
            <itemizedlist>
               <listitem>
                  <para>Create a directory <literal>endorsed</literal>
                                </para>
               </listitem>
               <listitem>
                  <para>Copy all jar files from eXist's <literal>lib/endorsed</literal> directory into the newly created
                                    directory</para>
               </listitem>
            </itemizedlist>
            <para>Specifying the
                            <literal>-Djava.endorsed.dirs=lib/endorsed</literal> system property on
                        the Java command line will force the JVM to prefer any library it finds in
                        the endorsed directory over its own system libraries. Copying the jars into
                            <literal>$JAVA_HOME/jre/lib/endorsed</literal> will do the same thing.
                        Note that the batch and shell scripts included with eXist all set the
                            <literal>java.endorsed.dirs</literal> system property to point to
                            <literal>lib/endorsed</literal>.</para>
         </listitem>
         <listitem>
            <para>Make sure your CLASSPATH includes these jar files.</para>
         </listitem>
      </orderedlist>
      <para>Internally, eXist has two different XML:DB driver implementations: the first
                communicates with a remote database engine using XML-RPC calls; the second has
                direct access to a local instance of eXist. Which implementation is selected depends
                on the XML:DB URI as described above. To access an embedded database, simply drop
                the host portion (i.e. <literal>localhost:8088/xmlrpc/</literal>) from the URI -
                for instance:</para>
      <programlisting>xmldb:exist:///db</programlisting>
      <para>To start an embedded database instance, simply set the system property <literal>exist.initdb</literal> to <literal>true</literal>. This will notify the XML:DB
                driver to read the configuration settings when starting the database if none has
                been previously started. For example, to launch your own Java application with an
                embedded instance, you may enter the command:</para>
      <programlisting>java -Dexist.initdb=true MyApp</programlisting>
      <para>The driver will try to read the eXist configuration file, create the required
                database files if they have not already been created, and launch the database. This
                of course implies that the driver should be able to read the configuration file
                    <literal>conf.xml</literal>. In fact, the driver looks for
                    <literal>conf.xml</literal> in the directory specified by the
                    <literal>exist.home</literal> system property. You should therefore ensure a copy
                of <literal>conf.xml</literal> is placed in the correct directory.
                For example, to launch your own application, while setting
                    <literal>exist.home</literal>, you may enter:</para>
      <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="listings/listing-23.txt"/>
      <important>
         <para>Please note that the paths to the data and log directories in <literal>conf.xml</literal> need to point to
                        <emphasis>writable</emphasis> directories.</para>
      </important>
      <para>Instead of using the <literal>-Dexist.initdb</literal> property, you can also tell
                the database driver to create a local database instance during the initialization of
                the XML:DB database manager. To do this, simply set the <literal>create-database</literal> property on the created
                    <literal>Database</literal> object to <literal>true</literal> - for
                example:</para>
      <example>
         <title>Initializing a Local Database Instance</title>
         <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="java" xlink:href="listings/listing-24.txt"/>
      </example>
      <para>When running eXist in embedded mode, you must ensure to properly shut down the
                database before your application exits. The main reason for this action is to flush
                all of the unwritten data buffers to disk. The database uses a background thread to
                periodically synchronize its buffers with the data files on the disk, and this
                thread will keep running if you don't shut down the database.</para>
      <para>There is a special XML:DB service, <literal>DatabaseInstanceManager</literal>,
                which contains a single method: <literal>shutdown</literal>. To properly shut
                down the database instance, retrieve the service from the <literal>/db-collection</literal> and call <literal>shutdown()</literal>. For
                example:</para>
      <example>
         <title>Shutting down the Database</title>
         <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="java" xlink:href="listings/listing-25.txt"/>
      </example>
      <para>To summarize, a minimal working class with a single static main to start/stop the
                db may look like this:</para>
      <example>
         <title>Minimal Example</title>
         <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" language="java" xlink:href="listings/listing-26.txt"/>
      </example>
      <para>Put this code into a Java file <literal>TestDB.java</literal> and store it into
                the target directory, which we already prepared above. Compile and run it
                with:</para>
      <programlisting xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="listings/listing-27.txt"/>
   </sect1>
</article>
<?xml-model href="http://docbook.org/xml/5.0/rng/docbook.rng"
        schematypens="http://relaxng.org/ns/structure/1.0"?><?xml-model href="http://docbook.org/xml/5.0/rng/docbook.rng" type="application/xml"
        schematypens="http://purl.oclc.org/dsdl/schematron"?><article xmlns="http://docbook.org/ns/docbook" version="5.0">
   <info>
      <title>HTTP-Related Functions in the request and session Modules</title>
      <date>October 2012</date>
      <keywordset>
         <keyword>TBD</keyword>
      </keywordset>
   </info>

   <!-- ================================================================== -->

   <sect1 xml:id="introduction">
      <title>Introduction</title>

      <para>The request module (in the <literal>http://exist-db.org/xquery/request</literal> function
                namespace) contains functions for handling HTTP request parameters. The session
                module (in the <literal>http://exist-db.org/xquery/session</literal> function namespace)
                contains functions for handling HTTP session variables. Functions in these
                namespaces are only usable if the query is executed through the XQueryGenerator or
                the XQueryServlet (for more information consult eXist-db's <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="devguide.xml">Developer's Guide</link> ).</para>
      <variablelist spacing="compact">
         <varlistentry>
            <term>request:get-parameter(<emphasis>name</emphasis>, <emphasis>default
                            value</emphasis>)</term>
            <listitem>
               <para>This HTTP function expects two arguments: the first denotes the name
                            of the parameter, the second specifies a default value, which is
                            returned if the parameter is not set. This function returns a sequence
                            containing the values for the parameter. The above script
                            (Adding/Subtracting Two Numbers) offers an example of how
                                <literal>request:get-parameter</literal> can be used to read HTTP
                            request parameters.</para>
            </listitem>
         </varlistentry>
         <varlistentry>
            <term>request:get-uri()</term>
            <listitem>
               <para>This function returns the URI of the current request. To encode this
                            URI using the current session identifier, use the following
                            function:</para>
               <programlisting>session:encode-url(request:get-uri())</programlisting>
            </listitem>
         </varlistentry>
         <varlistentry>
            <term>session:create()</term>
            <listitem>
               <para>This function creates a new HTTP session if none exists.</para>
               <para>Other session functions read and set session attributes, among other
                            operations. For example, an XQuery or Java object value can be stored in
                            a session attribute, to cache query results. For more example scripts,
                            please look at our <link xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="{demo}/examples/web/index.html">Examples</link> page,
                            under the XQuery Examples section.</para>
            </listitem>
         </varlistentry>
      </variablelist>
   </sect1>
</article>